<div class="card mb-3">
  <div class="card-body">
    <p>{{item.origin}}</p>
    <p>{{item.shortURL}}</p>
    <a href="/delete/{{item._id}}" class="btn btn-success">Delete</a>
    <a href="/edit/{{item._id}}" class="btn btn-success">Edit</a>
    <button data-short="{{item.shortURL}}" class="btn btn-success">Copy</button>
  </div>
</div>